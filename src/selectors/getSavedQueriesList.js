/* =================================================
============== Get Saved Queries List ================
================================================= */
export default (savedQueries) => {
  return savedQueries.map((savedQuery, index) => {
    return savedQuery;
  })
}